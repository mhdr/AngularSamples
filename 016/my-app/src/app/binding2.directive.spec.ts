import { Binding2Directive } from './binding2.directive';

describe('Binding2Directive', () => {
  it('should create an instance', () => {
    const directive = new Binding2Directive();
    expect(directive).toBeTruthy();
  });
});
